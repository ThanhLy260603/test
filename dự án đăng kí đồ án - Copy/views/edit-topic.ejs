<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Topic</title>
    <link rel="stylesheet" href="/css/edit-topic.css"> <!-- Add this line -->
</head>
<body>
    <header>
        <div class="header-container">
            <img src="/images/hinhbanner.png" alt="Logo" class="logo">
            <div class="title-container">
                <h1 class="title">Edit Topic</h1>
            </div>
            <a href="/topics">Quay Lại</a>
        </div>
    </header>
    
    <% if (success) { %> <!-- Kiểm tra xem success có tồn tại không -->
        <p class="success-message"><%= success %></p> <!-- Hiển thị thông báo thành công -->
    <% } %>
    <form action="/topics/<%= topic._id %>/edit" method="POST">
        <div>
            <label for="teacher">Teacher:</label>
            <input type="text" id="teacher" name="teacher" value="<%= topic.teacher %>">
        </div>
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= topic.title %>">
        </div>
        <div>
            <label for="note">Note:</label>
            <textarea id="note" name="note"><%= topic.note %></textarea>
        </div>
        <div>
            <label for="students">Students:</label>
            <input type="text" id="students" name="students" value="<%= topic.students %>">
        </div>
        <button type="submit">Save Changes</button>
    </form>
</body>
</html>
